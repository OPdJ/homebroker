{% extends 'base.html.twig' %}

{% block menu %}

{% endblock %}

{% block body %}


  <div class="row">
    <div class="col-6">
      <table class="table table-hover table-dark">
        <thead>
          <tr>
            <th scope="col" class="text-center">Ativ.</th>
            <th scope="col" class="text-center">Qtd.</th>
            <th scope="col" class="text-center">Compra</th>
            <th scope="col" class="text-center">Venda</th>
            <th scope="col" class="text-center">Qtd.</th>
            <th scope="col" class="text-center">Ativ.</th>
          </tr>
        </thead>
        <tbody>
        {% for ordem in ordensCompra %}
          <tr>
            <th scope="row" class="text-center">{{ ordem.tipoAtivo }}</th>
            <td class="text-center">{{ ordem.quantidade }}</td>
            <td class="verticalLine text-center">{{ ordem.preco|number_format(2, ',', '.') }}</td>
            <td class="text-center">5,34</td>
            <td class="text-center">20</td>
            <td class="text-center">XP</td>
          </tr>
        {% endfor %}
        </tbody>
      </table>
    </div>
    <div class="col">
      <div id="chart_div"></div>
    </div>
  </div> <!-- END_ROW -->

{% endblock %}